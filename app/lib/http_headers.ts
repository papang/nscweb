const http_headers = {
  "Content-Type": "application/json",
  "Access-Control-Allow-Origin": "*", // Mengizinkan semua origin
  "Access-Control-Allow-Methods": "GET, POST, PUT", // Mengizinkan metode HTTP yang diinginkan
  "Access-Control-Allow-Headers": "Content-Type, Authorization", // Mengizinkan header yang diinginkan
};

export default http_headers;